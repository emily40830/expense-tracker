<div class="container mt-5">
  <div class="flex-column">
    <!-- total expense-->
    <div class="row mb-3">
      <div class="col-lg-4 col-4">
        <div class="card h-70 dashCard">
          <div class="card-body d-flex align-items-center">
            <i class="far fa-money-bill-alt fa-3x text-secondary d-none d-sm-block"></i>
            <div class="text-center w-100">
              <div class="h5">Total</div>
              <div class="h3">$ {{sumOfamount}}</div>
            </div>
          </div>
        </div>
      </div>
      <div class="col-lg-4 col-4">
        <div class="card h-70 dashCard">
          <div class="card-body d-flex align-items-center">
            <i class="fas fa-pen-square fa-3x text-secondary d-none d-sm-block"></i>
            <div class="text-center w-100">
              <div class="h5">Count</div>
              <div class="h3">{{sumOfcount}}</div>
            </div>
          </div>
        </div>
      </div>
      <div class="col-lg-4 col-4">
        <div class="card h-70 dashCard">
          <div class="card-body d-flex align-items-center">

            <i class="fas fa-percentage fa-3x text-secondary d-none d-sm-block"></i>
            <div class="text-center w-100">
              <div class="h5">Percentage</div>
              <div class="h3">{{percent}} %</div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- create and filter -->
    <div class="row mt-5 mb-3 ml-1 mr-3 justify-content-between">
      <form class="form mr-3" action="/create" method="GET">
        <button class="btn btn-info" type="submit">Create</button>
      </form>
      <form action="/" method="GET" class="row">
        <div class="date-select mr-3">
          <label for="year_month">Year-month:</label>
          <input id="year_month" type="month" name="year_month" min="2019-09" max="{{maxYm}}"
            onchange="this.form.submit();" value={{{selectDate}}}>
        </div>
        <div class="category-select">
          <select class="form-control" style="cursor: pointer" name="category_id" onchange="this.form.submit()">
            <option value="" selected>All</option>
            {{#each categories}}
            <option value="{{this.category_id}}" {{#if isSelected}} selected{{/if}}>
              {{this.categoryName}}</option>
            {{/each}}
          </select>
        </div>




      </form>
    </div>

    <!-- records list group  start-->

    {{!-- <ul class="list-group">
      {{#each records}}
      <li class="list-group-item eachItem mb-2">
        <div class="list-box row d-flex justify-content-between align-items-center">
          <div class="list-info col-auto d-flex align-items-center flex-grow-1">
            <div class="list-icon mr-5">
              <i class="{{this.categoryInfo.categoryIcon}} fa-3x"></i>
            </div>
            <div class="list-text flex-grow-1 mx-2">
              <p class="item">{{this.name}} {{this.merchandise}}</p>
              <p class="date">{{this.date}}</p>
            </div>
            <div class="list-expense">
              {{this.amount}}
            </div>
          </div>
          <div class="list-button col-auto">
            <a class="btn edit-button" href="/record/{{this._id}}" style="display: inline;">
              <i class="fas fa-edit fa-ml d-none d-sm-block"></i>">
            </a>
          </div>
          <div class="list-button col-auto">
            <form action="./record/{{this._id}}?_method=DELETE" method="post" style="display: inline;">
              <button type="submit" class="delete fas fa-trash fa-ml"></button>
            </form>
          </div>

      </li>
      {{/each}}
    </ul> --}}

    <!-- records list group -->

    <!-- records table -->
    <table class="table mt-5 eachItem">
      <thead>
        <tr>
          <th scope="col"></th>
          <th scope="col">Name</th>
          <th scope="col">Date</th>
          <th scope="col">Amount</th>
          <th scope="col">Edit</th>
        </tr>
      </thead>
      <tbody>
        {{#each records}}
        <tr class="eachItem">
          <th scope="row"><i class="{{this.categoryInfo.categoryIcon}} fa-3x"></i></th>
          <td>
            <p class="item">{{this.name}}</p>
            <p class="merchandise">{{this.merchandise}}</p>
          </td>
          <td>{{this.date}}</td>
          <td>{{this.amount}}</td>
          <td>
            <div class="d-flex text-align" style="margin: auto;">

              <a class="btn mr-2" href="/record/{{this._id}}"
                style="margin-top: auto;margin-bottom: auto;padding:0px; ">
                <i class="fas fa-edit fa-ml d-none d-sm-block"></i>
              </a>
              <form action="./record/{{this._id}}?_method=DELETE" method="post" class="ml-2"
                style="margin: 0; padding:0px;">

                <button type="submit" class="delete fas fa-trash fa-ml"></button>
              </form>
              {{!-- </div> --}}
            </div>
          </td>
        </tr>

        {{/each}}
      </tbody>
    </table>

  </div>
</div>